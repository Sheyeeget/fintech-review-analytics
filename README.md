# Fintech Review Analytics

Analyzing Google Play Store reviews for Ethiopian banks.

## Setup

1. Create virtual environment: `python -m venv venv`
2. Activate: `source venv/bin/activate` (or `venv\Scripts\activate` on Windows)
3. Install: `pip install -r requirements.txt`
4. Download spaCy model: `python -m spacy download en_core_web_sm`

## Running the pipeline

(Detailed steps will be added after implementation)